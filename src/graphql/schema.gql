# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

type Query {
  game(id: ID!): Game!
  getAllGames: [Game!]!
  user(id: ID!): User!
  review(gameId: ID!, reviewerId: ID!): Review!
}

type Game {
  id: ID!
  title: String!
  price: Float!
  reviews: [Review!]!
  averageRating: Float
}

type Review {
  id: ID!
  game: Game!
  rating: Int!
  comment: String!
  reviewer: User!
  dateCreated: DateTime!
}

type User {
  id: ID!
  name: String!
  gamesInLibrary: [Game!]!
  reviews: [Review!]!
}

# The javascript `Date` as string. Type represents date and time as the ISO Date string.
scalar DateTime

type Mutation {
  addGame(input: NewGameInput!): Game!
  addUser(input: NewUserInput!): User!
  purchaseGame(input: GamePurchaseInput!): User!
  addReview(input: NewReviewInput!): Review!
}

input NewGameInput {
  title: String!
  price: Float!
}

input NewUserInput {
  name: String!
}

input GamePurchaseInput {
  gameId: ID!
  userId: ID!
}

input NewReviewInput {
  gameId: ID!
  reviewerId: ID!
  rating: Int!
  comment: String!
}

type Subscription {
  newReviewAdded(subscribedGameIds: [ID!]!): Review!
}
